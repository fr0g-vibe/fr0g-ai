# fr0g-ai-registry Configuration
# Performance Optimized Settings

# Server Configuration
REGISTRY_HOST=0.0.0.0
REGISTRY_PORT=8500

# Redis Configuration for Persistence
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# Cache Configuration (handled in code)
# LRU_CACHE_SIZE=1000
# LRU_CACHE_TTL=30s

# Health Check Configuration
HEALTH_CHECK_INTERVAL=30s
SERVICE_TIMEOUT=2m

# Performance Settings
READ_TIMEOUT=10s
WRITE_TIMEOUT=10s
IDLE_TIMEOUT=60s
